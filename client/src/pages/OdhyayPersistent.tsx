/* ODHYAY style: Quiet Editorial — persisted records enter the same calm, literary composition used for the public library. */
import { useEffect, useMemo, useState } from "react";
import { Link, useRoute } from "wouter";
import { ArrowLeft, ArrowRight, Bookmark, ChevronRight, Expand, Filter, Heart, Minus, Plus, Search, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { useAuth } from "@/_core/hooks/useAuth";
import { BookGrid, Mark, PageFrame, SearchBar, SectionLabel } from "@/components/OdhyayShell";
import { assets, type Book } from "@/lib/odhyayData";
import { trpc } from "@/lib/trpc";

type RecordBook = { id: number; title: string; slug: string; description: string; coverUrl: string | null; pdfKey: string | null; pageCount: number; status: "draft" | "published"; authorName: string; categoryName: string | null; categorySlug: string | null };
const fallbackCover = assets.cover1;
const toViewBook = (book: RecordBook): Book => ({ slug: book.slug, title: book.title, author: book.authorName, category: book.categoryName ?? "Other", pages: book.pageCount || 1, cover: book.coverUrl || fallbackCover, description: book.description });
function QueryNotice({ loading, error, empty }: { loading: boolean; error: unknown; empty: string }) { if (loading) return <div className="animate-pulse border border-[#332d39] bg-[#151219] px-6 py-12 text-sm text-[#8f8996]">Preparing the shelves…</div>; if (error) return <div className="border border-[#5c3a48] bg-[#25171f] px-6 py-12 text-sm text-[#d7b7c2]">The library could not be reached. Please refresh and try again.</div>; return <div className="border border-dashed border-[#4a4052] px-6 py-16 text-center"><Mark /><h2 className="font-display mt-5 text-3xl">{empty}</h2><p className="mt-3 text-sm text-[#8f8996]">Your first published book will appear here.</p></div>; }

export function HomePersistentPage() {
  const library = trpc.library.list.useQuery(); const categories = trpc.library.categories.useQuery(); const items = (library.data ?? []).map(toViewBook);
  const featured = items[0];
  return <PageFrame><main><section className="relative min-h-[650px] overflow-hidden border-b hairline"><img src={assets.hero} alt="A quiet reading room" className="absolute inset-0 h-full w-full object-cover opacity-70" /><div className="hero-vignette absolute inset-0" /><div className="relative container flex min-h-[650px] items-center py-24"><div className="max-w-[680px]"><p className="eyebrow text-amethyst">A digital library for curious minds</p><h1 className="font-display mt-7 text-[clamp(3.8rem,9vw,7.8rem)] leading-[.91] tracking-[-.04em]">Read.<br /><span className="text-[#b7a4d7]">Discover.</span><br />Grow.</h1><p className="mt-8 max-w-md text-base leading-8 text-[#c1bac5]">A calm place to read. Find the next page worth your time, and let the rest of the world go quiet for a while.</p><div className="mt-10 flex flex-wrap gap-5"><Link href="/library" className="focus-ring inline-flex items-center gap-3 bg-[#b7a4d7] px-5 py-3 text-xs font-bold uppercase tracking-[.15em] text-[#17121c]">Explore Library <ArrowRight size={15} /></Link><Link href="/categories" className="focus-ring inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.15em] text-[#e2dce5] hover:text-amethyst">Browse Categories <ChevronRight size={15} /></Link></div><div className="mt-16"><SearchBar /></div></div></div></section>
    <section className="container py-24 lg:py-32"><SectionLabel number="01">Featured book</SectionLabel>{featured ? <div className="grid gap-12 lg:grid-cols-[minmax(0,1fr)_1.25fr] lg:items-center lg:gap-24"><Link href={`/book/${featured.slug}`} className="focus-ring group relative mx-auto block w-full max-w-[360px] lg:max-w-none"><div className="aspect-[2/3] overflow-hidden bg-[#25212b] cover-shadow"><img src={featured.cover} alt={`${featured.title} cover`} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.02]" /></div></Link><div className="max-w-[600px]"><p className="eyebrow text-[#8f8996]">Editor’s shelf · {featured.pages} pages</p><h2 className="font-display mt-5 text-[clamp(2.8rem,5vw,5.4rem)] leading-[.95]">{featured.title}</h2><p className="mt-6 text-sm leading-8 text-[#a9a1ad]">{featured.description}</p><div className="mt-8 flex items-center gap-4 text-xs text-[#8f8996]"><span>{featured.author}</span><span className="h-1 w-1 rounded-full bg-[#b7a4d7]" /><span>{featured.category}</span></div><Link href={`/read/${featured.slug}`} className="focus-ring mt-10 inline-flex items-center gap-3 border-b border-[#b7a4d7] pb-2 text-xs font-bold uppercase tracking-[.17em]">Read now <ArrowRight size={15} /></Link></div></div> : <QueryNotice loading={library.isLoading} error={library.error} empty="Your library is waiting for its first chapter." />}</section>
    <section className="border-y hairline bg-[#151219] py-24"><div className="container"><SectionLabel number="02">Recently added</SectionLabel>{items.length ? <BookGrid items={items.slice(0, 4)} /> : <QueryNotice loading={library.isLoading} error={library.error} empty="No books have been published yet." />}</div></section>
    <section className="container py-24 lg:py-32"><div className="grid gap-14 lg:grid-cols-[1fr_2fr] lg:gap-24"><div><SectionLabel number="03">Find your shelf</SectionLabel><h2 className="font-display text-[clamp(2.6rem,4vw,4.4rem)] leading-[.97]">Read by<br /><span className="text-amethyst">curiosity.</span></h2></div><div className="grid grid-cols-2 border-t hairline sm:grid-cols-3">{(categories.data ?? []).slice(0, 9).map((category, index) => <Link href={`/search?q=${encodeURIComponent(category.name)}`} key={category.id} className="focus-ring group flex min-h-[88px] items-center justify-between border-b hairline pr-3 text-sm text-[#c9c1ce] hover:text-amethyst"><span>{category.name}</span><span className="text-[.65rem] text-[#716a79]">{String(index + 1).padStart(2, "0")}</span></Link>)}{!categories.isLoading && !categories.data?.length && <p className="col-span-full py-8 text-sm text-[#8f8996]">Categories will appear as books are added.</p>}</div></div></section></main></PageFrame>;
}

export function LibraryPersistentPage() { const [categorySlug, setCategorySlug] = useState<string>(); const categories = trpc.library.categories.useQuery(); const library = trpc.library.list.useQuery(categorySlug ? { categorySlug } : undefined); const items = (library.data ?? []).map(toViewBook); return <PageFrame><main className="container py-16 lg:py-24"><div className="flex flex-col justify-between gap-10 border-b hairline pb-12 lg:flex-row lg:items-end"><div><p className="eyebrow text-amethyst">The library / 01</p><h1 className="font-display mt-5 text-[clamp(3.6rem,8vw,7rem)] leading-[.92]">Every book,<br /><span className="text-[#81788c]">a doorway.</span></h1></div><p className="max-w-sm text-sm leading-7 text-[#948c9b]">A growing shelf of books selected for their ability to make a little more room in your day.</p></div><div className="flex flex-wrap items-center gap-3 py-8"><Filter size={16} className="text-amethyst" /><button onClick={() => setCategorySlug(undefined)} className={`focus-ring px-3 py-2 text-xs font-semibold ${!categorySlug ? "bg-[#b7a4d7] text-[#17121c]" : "text-[#958d9b]"}`}>All books</button>{(categories.data ?? []).slice(0, 6).map((category) => <button key={category.id} onClick={() => setCategorySlug(category.slug)} className={`focus-ring px-3 py-2 text-xs font-semibold ${categorySlug === category.slug ? "bg-[#b7a4d7] text-[#17121c]" : "text-[#958d9b]"}`}>{category.name}</button>)}</div><div className="mb-8 flex items-center justify-between border-t hairline pt-5"><span className="text-xs text-[#837b8b]">{items.length} books in the collection</span><Link href="/search" className="focus-ring flex items-center gap-2 text-xs font-semibold text-[#b7a4d7]">Search the library <Search size={14} /></Link></div>{items.length ? <BookGrid items={items} /> : <QueryNotice loading={library.isLoading} error={library.error} empty="Your library is waiting for its first chapter." />}</main></PageFrame>; }

export function CategoriesPersistentPage() { const categories = trpc.library.categories.useQuery(); return <PageFrame><main className="container py-16 lg:py-24"><div className="max-w-3xl"><p className="eyebrow text-amethyst">The library / 02</p><h1 className="font-display mt-5 text-[clamp(3.8rem,8vw,7.3rem)] leading-[.91]">Follow a<br /><span className="text-[#81788c]">thread.</span></h1><p className="mt-8 max-w-lg text-base leading-8 text-[#a9a1ad]">Some days begin with a story. Some with a question. Choose a direction and see where it takes you.</p></div><div className="mt-20 grid border-t hairline sm:grid-cols-2 lg:grid-cols-3">{(categories.data ?? []).map((category, index) => <Link key={category.id} href={`/search?q=${encodeURIComponent(category.name)}`} className="focus-ring group min-h-[150px] border-b hairline p-7 hover:bg-[#19151e]"><div className="flex items-start justify-between"><Mark small /><span className="text-[.65rem] text-[#6f6876]">{String(index + 1).padStart(2, "0")}</span></div><div className="mt-9 flex items-end justify-between"><h2 className="font-display text-[1.65rem] group-hover:text-amethyst">{category.name}</h2><ChevronRight size={17} className="text-[#6f6876]" /></div></Link>)}</div>{!categories.isLoading && !categories.data?.length && <div className="mt-8"><QueryNotice loading={false} error={categories.error} empty="The first category is still waiting." /></div>}</main></PageFrame>; }

export function SearchPersistentPage() { const initial = new URLSearchParams(window.location.search).get("q") ?? ""; const library = trpc.library.list.useQuery(initial ? { query: initial } : undefined); const items = (library.data ?? []).map(toViewBook); return <PageFrame><main className="container min-h-[720px] py-16 lg:py-24"><div className="max-w-3xl"><p className="eyebrow text-amethyst">Search the shelves</p><h1 className="font-display mt-5 text-[clamp(3.6rem,8vw,7rem)] leading-[.92]">What are you<br /><span className="text-[#81788c]">looking for?</span></h1><div className="mt-10"><SearchBar compact defaultValue={initial} /></div></div><div className="mt-20 border-t hairline pt-6"><div className="mb-8 flex items-center justify-between"><span className="text-xs text-[#8d8594]">{initial ? `${items.length} results for “${initial}”` : "Showing all books"}</span><Link href="/search" className="focus-ring text-xs font-semibold text-[#b7a4d7]">Clear search</Link></div>{items.length ? <BookGrid items={items} /> : <QueryNotice loading={library.isLoading} error={library.error} empty="No books found." />}</div></main></PageFrame>; }

export function BookPersistentPage() { const [, params] = useRoute("/book/:slug"); const slug = params?.slug; const detail = trpc.library.getBySlug.useQuery({ slug: slug ?? "route-pending" }, { enabled: Boolean(slug) }); const book = detail.data as RecordBook | undefined; const favorite = trpc.reader.toggleFavorite.useMutation({ onSuccess: (result) => toast.success(result.favorite ? "Added to favorites." : "Removed from favorites."), onError: () => toast.error("Please sign in to manage favorites.") }); if (!slug || detail.isLoading) return <PageFrame><main className="container py-24"><QueryNotice loading error={null} empty="" /></main></PageFrame>; if (!book) return <PageFrame><main className="container py-24"><QueryNotice loading={false} error={detail.error} empty="This book is not available." /></main></PageFrame>; const view = toViewBook(book); return <PageFrame><main className="container py-14 lg:py-24"><Link href="/library" className="focus-ring inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[.14em] text-[#8f8996]"><ArrowLeft size={15} /> Back to library</Link><div className="mt-12 grid gap-12 lg:grid-cols-[minmax(260px,420px)_1fr] lg:items-center lg:gap-24"><div className="mx-auto w-full max-w-[390px]"><div className="aspect-[2/3] overflow-hidden bg-[#24202a] cover-shadow"><img src={view.cover} alt={`${view.title} cover`} className="h-full w-full object-cover" /></div></div><div className="max-w-2xl"><p className="eyebrow text-amethyst">{view.category} · {view.pages} pages</p><h1 className="font-display mt-6 text-[clamp(3rem,7vw,6.7rem)] leading-[.92]">{view.title}</h1><p className="mt-6 text-lg text-[#a9a1ad]">{view.author}</p><div className="my-8 h-px w-full bg-[#332d39]" /><p className="max-w-xl text-base leading-8 text-[#b7afbb]">{view.description}</p><div className="mt-10 flex flex-wrap gap-6"><Link href={`/read/${view.slug}`} className="focus-ring inline-flex items-center gap-3 bg-[#b7a4d7] px-5 py-3 text-xs font-bold uppercase tracking-[.15em] text-[#17121c]">Read now <ArrowRight size={15} /></Link><button onClick={() => favorite.mutate({ bookId: book.id })} className="focus-ring inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.15em] text-[#d1c8d5]"><Heart size={15} /> Add to favorites</button></div></div></div></main></PageFrame>; }

function ReaderToolbar({ page, pages, zoom, theme, onPage, onZoom, onTheme, onBookmark }: { page: number; pages: number; zoom: number; theme: string; onPage: (page: number) => void; onZoom: (zoom: number) => void; onTheme: (theme: string) => void; onBookmark: () => void }) { return <div className="fixed bottom-0 left-0 right-0 z-40 border-t border-[#3d3547] bg-[#17141c]/95 px-3 py-3 backdrop-blur-xl md:bottom-5 md:left-1/2 md:right-auto md:w-auto md:-translate-x-1/2 md:rounded-sm md:border"><div className="flex items-center justify-center gap-1 text-[#d7cedb]"><button className="focus-ring p-2 disabled:opacity-30" onClick={() => onPage(Math.max(1, page - 1))} disabled={page === 1}><ArrowLeft size={16} /></button><span className="mx-2 border-x border-[#3a3242] px-3 text-xs">{page} <span className="text-[#746c7d]">/ {pages}</span></span><button className="focus-ring p-2 disabled:opacity-30" onClick={() => onPage(Math.min(pages, page + 1))} disabled={page === pages}><ArrowRight size={16} /></button><button className="focus-ring hidden p-2 sm:block" onClick={() => onZoom(Math.max(.8, zoom - .1))}><Minus size={16} /></button><button className="focus-ring hidden p-2 sm:block" onClick={() => onZoom(Math.min(1.35, zoom + .1))}><Plus size={16} /></button><button className="focus-ring p-2" onClick={() => onTheme(theme === "dark" ? "daylight" : theme === "daylight" ? "sepia" : "dark")}><Sparkles size={16} /></button><button className="focus-ring hidden p-2 sm:block" onClick={() => document.documentElement.requestFullscreen?.()}><Expand size={16} /></button><button className="focus-ring hidden p-2 text-amethyst sm:block" onClick={onBookmark}><Bookmark size={16} /></button></div></div>; }

export function ReaderPersistentPage() { const [, params] = useRoute("/read/:slug"); const slug = params?.slug; const detail = trpc.library.getBySlug.useQuery({ slug: slug ?? "route-pending" }, { enabled: Boolean(slug) }); const { isAuthenticated } = useAuth(); const saveProgress = trpc.reader.saveProgress.useMutation(); const saveBookmark = trpc.reader.addBookmark.useMutation({ onSuccess: () => toast.success("Bookmark saved."), onError: () => toast.error("Please sign in to save bookmarks.") }); const [page, setPage] = useState(1); const [zoom, setZoom] = useState(1); const [theme, setTheme] = useState(() => localStorage.getItem("odhyay-reader-theme") || "dark"); const book = detail.data as RecordBook | undefined; useEffect(() => { localStorage.setItem("odhyay-reader-theme", theme); }, [theme]); useEffect(() => { if (book && isAuthenticated) saveProgress.mutate({ bookId: book.id, currentPage: page, progressPercentage: Math.round((page / Math.max(1, book.pageCount)) * 100) }); }, [page]); if (!slug || !book) return <PageFrame><main className="container py-24"><QueryNotice loading={detail.isLoading} error={detail.error} empty="This reading room is not available." /></main></PageFrame>; const pages = Math.max(1, book.pageCount); const themeClass = theme === "sepia" ? "bg-[#3b3026]" : theme === "daylight" ? "bg-[#d9d0bd]" : "bg-[#111015]"; const paperClass = theme === "sepia" ? "bg-[#e1cfaa] text-[#483d32]" : theme === "daylight" ? "bg-[#eee8da] text-[#413c38]" : "bg-[#211e25] text-[#d9d2d8]"; return <div className={`min-h-screen ${themeClass}`}><header className="flex h-[66px] items-center justify-between border-b border-white/10 bg-[#111015]/90 px-4 text-[#eee9f0] md:px-8"><Link href={`/book/${book.slug}`} className="focus-ring flex items-center gap-3 text-xs font-semibold"><ArrowLeft size={16} /><span className="hidden sm:inline">Back to book</span></Link><div className="flex items-center gap-3"><Mark small /><span className="font-display text-lg tracking-[.14em]">ODHYAY</span></div><span className="text-xs text-[#938b9c]">{theme === "dark" ? "Night" : theme === "daylight" ? "Daylight" : "Sepia"}</span></header><main className="flex min-h-[calc(100vh-66px)] flex-col items-center px-4 pb-28 pt-10 md:pt-16"><div className="mb-7 flex w-full max-w-[760px] justify-between text-[.62rem] font-bold uppercase tracking-[.16em] text-white/45"><span>Reading · {book.categoryName ?? "Other"}</span><span>{Math.round((page / pages) * 100)}% complete</span></div><article className={`reader-paper w-full max-w-[760px] px-7 py-12 sm:px-14 sm:py-16 lg:px-24 lg:py-24 ${paperClass}`} style={{ transform: `scale(${zoom})`, transformOrigin: "top center", marginBottom: `${(zoom - 1) * 850}px` }}><p className="text-[.63rem] font-bold uppercase tracking-[.2em] opacity-60">{book.categoryName ?? "Other"} · Chapter {String(page).padStart(2, "0")}</p><h1 className="font-display mt-8 text-4xl leading-tight sm:text-5xl">{book.title}</h1><p className="mt-4 text-sm opacity-65">{book.authorName}</p><div className="my-12 border-t border-current/15" /><p className="font-display text-[1.14rem] leading-[2] sm:text-[1.3rem]">{book.description}</p><p className="mt-10 font-display text-[1.14rem] leading-[2] sm:text-[1.3rem]">This reader is ready for your stored PDF file. Once a PDF key is attached through the admin upload flow, this calm reading space can render it while keeping progress and bookmarks in your account.</p><div className="mt-24 flex justify-between border-t border-current/15 pt-5 text-[.63rem] font-bold uppercase tracking-[.16em] opacity-55"><span>ODHYAY</span><span>Page {page}</span></div></article></main><ReaderToolbar page={page} pages={pages} zoom={zoom} theme={theme} onPage={setPage} onZoom={setZoom} onTheme={setTheme} onBookmark={() => saveBookmark.mutate({ bookId: book.id, pageNumber: page })} /></div>; }
